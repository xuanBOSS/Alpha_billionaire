<Window x:Class="ChessGame.Client.Views.MatchRequest"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ChessGame.Client.Views"
        mc:Ignorable="d"
        Title="MatchRequest" Height="200" Width="300"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height = "1*" />
            <RowDefinition Height = "1*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text = "请稍等" FontFamily="FZYaoTi" Foreground="Sienna" FontSize = "26" 
           HorizontalAlignment="Center" Margin="0,45,0,10"/>
        <TextBlock Grid.Row="1" Text = "正在进行匹配..." FontFamily="FZYaoTi" Foreground="Sienna" FontSize = "26" 
                   HorizontalAlignment="Center" VerticalAlignment = "Top"/>
        
    </Grid>
</Window>
